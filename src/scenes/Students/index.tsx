import { observer } from 'mobx-react';
import * as React from 'react';

@observer
class StudentsScene extends React.Component {
    render() {
        return(
            <div>
                Students Scene
            </div>
        )
    }
}

export default StudentsScene